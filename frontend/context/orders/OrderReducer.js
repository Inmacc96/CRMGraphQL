import {
  SELECT_CUSTOMER,
  SELECT_PRODUCT,
  QUANTITY_PRODUCTS,
} from "../../types";

const OrderReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default OrderReducer;
